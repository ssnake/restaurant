<%= form_with(model: group, local: true) do |form| %>

  <div class="field">
    <%= form.label :name, 'Название группы' %>
    <%= form.text_field :name %>
  </div>


  <div class="actions">
    <%= form.submit 'Создать' %>
  </div>
<% end %>
