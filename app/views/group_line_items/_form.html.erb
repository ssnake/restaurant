<%= form_with(model: group_line_item, local: true) do |form| %>
  <% if group_line_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group_line_item.errors.count, "error") %> prohibited this group_line_item from being saved:</h2>

      <ul>
      <% group_line_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :group_line_item_product_id %>
  </div>

  <div class="field">
    <%= form.label :group_cart_id %>
    <%= form.text_field :group_cart_id, id: :group_line_item_group_cart_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
