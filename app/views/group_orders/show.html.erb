<h2>Детализация группового</h2>

ID заказа: <%= @group_order.id %><br/>
Группа заказа: <%= @group_order.group_name %><br/>
Создатель группы заказа: <%= @group_order.name %> | <%= @group_order.email %><br/>
Примечание к заказу: <%= @group_order.notice %><br/>
Тип оплаты: <%= @group_order.pay_type %><br/>
Когда заказ создан: <%= @group_order.created_at %><br/>
<%= link_to 'Просмотр по заказанным блюдам', group_order_path(group_id: @group_order.group_id, 
    id: @group_order.id ), class: "btn btn-primary btn-sm", style: "color: white" %> | 
<%= link_to 'Просмотр по заказчикам', group_order_path(group_id: @group_order.group_id, 
    id: @group_order.id, how_to_view: 'by_user'), class: "btn btn-primary btn-sm", style: "color: white" %><br/>
----------------------------------<br/>

<% if params[:how_to_view] == 'by_user' %>
  <%= render 'group_orders_admin/show_by_users' %>
<% else %>
  <%= render 'group_orders_admin/show_by_dish' %>
<% end %>


<%= link_to 'На главную', root_path %>

